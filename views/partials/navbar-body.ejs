
</head>
<body id="myPage" class="Site">
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-yummyli" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a href="/" class="navbar-brand smoothScroll"><img class="hat" src="/img/hat_logo.png" /><span class="logo">yummy.li</span></a>
        </div>
        <div class="collapse navbar-collapse" id="nav-yummyli">
          <ul class="nav navbar-nav">
            <li><a href="/team">About</a></li>
            <li><a href="/trending">Trending Recipes</a></li>
            <li><a href="#" data-toggle="modal" data-target="#howitworks">How it works</a></li>
            <li class="divider-vertical"></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if(!currentUser){ %>
		          <li><a href="/auth/login">Log in</a></li>
		          <li><a href="/auth/signup">Sign Up</a></li>
		        <% } else { %>
              <li>
                <% if(currentUser.image == undefined){ %>
                      <img class="profile-img" src="/img/profile_user.jpg">
                  <% } else { %>
                      <img class="profile-img" src="<%= currentUser.image %>">
                  <% } %>
              </li>
              <li>
                <a href="/profile/<%= currentUser._id %>"><%= currentUser.name %> <%= currentUser.surname %></a>
              </li>
                <% num = currentUser.recipes.length %>
                <% if(num > 0) { %>
                    <li>
                      <a href="/profile/<%= currentUser._id %>/wishlist">
                        <span><i class="fa fa-heart-o" aria-hidden="true"></i> My Wishlist ( <%= num %> )</span></a>
                    </li>
                    <li>
                      <a href="/auth/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li>
                      <a href="/profile/<%= currentUser._id %>/wishlist"><span><i class="fa fa-heart-o" aria-hidden="true"></i></span> My Wishlist</a>
                    </li>
                    <li>
                      <a href="/auth/logout">Logout</a>
                    </li>
                <% } %>    
            <% } %>
        </div>
      </div>
    </nav>
  <main class="Site-content">  